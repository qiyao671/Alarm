<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".RequestAlarmReceiver" >
        </receiver>

        <activity
            android:name=".NewActivity"
            android:label="@string/title_activity_new" >
        </activity>
        <activity
            android:name=".NumberActivity"
            android:label="@string/title_activity_number" >
        </activity>

        <receiver android:name=".RepeatingAlarmReceiver" >
        </receiver>

        <activity
            android:name=".Alarm_run"
            android:label="@string/title_activity_alarm_run" >
        </activity>
        <activity
            android:name=".Alarm_voice"
            android:label="@string/title_activity_alarm_voice" >
        </activity>
    </application>

</manifest>
